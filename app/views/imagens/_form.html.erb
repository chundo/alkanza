<%= form_with(model: imagen, local: true) do |form| %>
  <% if imagen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(imagen.errors.count, "error") %> prohibited this imagen from being saved:</h2>

      <ul>
      <% imagen.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :imagen_url %>
  </div>

  <div class="field">
    <%= form.label :sitio %>
    <%= form.text_field :sitio, id: :imagen_sitio %>
  </div>

  <div class="field">
    <%= form.label :palabra %>
    <%= form.text_field :palabra, id: :imagen_palabra %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
